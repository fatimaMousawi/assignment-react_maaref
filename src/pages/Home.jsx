import {Link} from 'react-router-dom';import useProducts from '../hooks/useProducts';import ProductList from '../components/ProductList';
export default function Home(){const{products,loading,error}=useProducts();return <section className="container"><div className="hero"><div><span>WELCOME TO NOVASHOP</span><h1>Simple shopping, built with React.</h1><p>Browse products loaded from an API and manage your cart with Redux.</p><Link className="primary" to="/products">Explore Products</Link></div><div className="hero-icon">🛍️</div></div><h2 className="section-title">Featured Products</h2>{loading&&<div className="status">Loading products...</div>}{error&&<div className="status error">{error}</div>}{!loading&&!error&&<ProductList products={products.slice(0,4)}/>}</section>}
